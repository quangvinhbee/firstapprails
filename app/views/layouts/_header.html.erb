<nav class="navbar navbar-expand-lg navbar-light bg-light full-width">
            <div class="container px-4 px-lg-5">
                <a class="navbar-brand" href="#!">BookStore</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                        <li class="nav-item"><a class="nav-link active" aria-current="page" href="/">Trang chủ</a></li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Lựa chọn</a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li> <%= link_to "Danh sách tác giả", authors_path, class:'dropdown-item' %></li>
                                <li> <%= link_to "Danh sách sách", books_path, class:'dropdown-item' %></li>
                                <li> <%= link_to "Danh sách danh mục", categories_path, class:'dropdown-item' %></li>
                            </ul>
                        </li>
                    </ul>
                    <form class="d-flex">
                        <button class="btn btn-outline-dark" type="submit">
                            <i class="bi-cart-fill me-1"></i>
                               <%= link_to  'Giỏ hàng',cards_path%>
                            <span class="badge bg-dark text-white ms-1 rounded-pill">0</span>
                        </button>
                    </form>
                </div>
                <div class="px-4">
                    <%if current_user %>
                    <%= link_to 'Đăng xuất' , destroy_user_session_path,method: :delete%>
                    <%else%>
                    <%= link_to 'Đăng kí' , new_user_registration_path%>
                    <%= link_to 'Đăng nhập' , new_user_session_path%>
                    <%end%>
                </div>
            </div>
        </nav>